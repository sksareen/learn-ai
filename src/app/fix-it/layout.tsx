import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Module 3: Fix It, Don't Restart — Learn AI",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
