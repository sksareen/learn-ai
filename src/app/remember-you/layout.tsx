import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Module 4: Make It Remember You — Learn AI",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
