import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Module 5: Build Your First Tool — Learn AI",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
